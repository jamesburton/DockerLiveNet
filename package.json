{
    "scripts": {
        "start": "docker-compose -f docker-compose.Development.yml up -d --build",
        "stop": "docker-compose -f docker-compose.Development.yml stop",
        "rm": "docker-compose -f docker-compose.Development.yml rm",
        "test": "echo \"Error: no test specified\" && exit 1",
        "production": "docker-compose up -d --build",
        "production:stop": "docker-compose stop",
        "production:rm": "docker-compose rm"
    }
}